const assert = require('chai').assert;
//const sinon = require('sinon');

describe('<%= moduleName %>', function () {
  it('does things', function () {
    const things = 'good';
    assert.notEqual(things, 'bad');
    assert.equal(things, 'good');
  });
});
